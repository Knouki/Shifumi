apply plugin: 'java'

repositories {
    maven {
        url 'http://repo1.maven.org/maven2'
    }
}

dependencies {
    testCompile 'junit:junit:4.12'
}

jar {
    baseName = 'shifumi'
    version =  '0.1.0'
}